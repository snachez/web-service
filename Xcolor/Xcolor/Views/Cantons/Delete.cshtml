@model Xcolor.Models.Canton

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Canton</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.canton1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.canton1)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Provincia.provincia1)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Provincia.provincia1)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
